# emergence - Claude Code Pointer

Claude Code should use `AGENTS.md` as the current project guide.

This file is intentionally thin to avoid duplicate agent instructions drifting apart. Update `AGENTS.md` first when the repo structure, build path, deployment workflow, content collections, or project status contract changes.
