# Sung-Min Park Portfolio — Design Direction

## 목표

이 사이트는 연구자를 설명하는 장식보다 **프로젝트 자체를 빠르게 읽게 하는
포트폴리오**를 목표로 한다. 시각 언어는 학술 문서와 에디토리얼 레이아웃에서
가져오되, 특정 템플릿을 그대로 복제하지 않는다.

## 정보 구조

프로젝트에는 서로 다른 두 축이 있다.

1. `project_type`은 프로젝트의 성격을 나타내며 페이지의 주 분류로 사용한다.
   - `research`: research implementations
   - `systems`: software & design systems
2. `status`는 완성도를 나타내며 각 프로젝트 안의 보조 정보로 사용한다.
   - `completed`
   - `in-progress`
   - `planned`

각 유형에서 `order`가 가장 앞선 프로젝트를 파란 테두리로 표현한다. 모든
프로젝트의 카드 크기와 제목 크기는 통일하고, 별도의 “featured” 설명 없이
순서와 색으로 중요도를 드러낸다.

## 시각 원칙

### 1. 프로젝트가 주인공이다

- 홈페이지 히어로는 이름 소개가 아니라 `projects.`로 시작한다.
- 유형 제목 다음에 프로젝트가 바로 이어진다.
- 분류 기준을 설명하는 별도 안내 문단이나 인덱스 패널을 두지 않는다.

### 2. 장식을 줄이고 위계를 강화한다

- 카드 배경, 글로우, 큰 아이콘 썸네일을 사용하지 않는다.
- 얇은 구분선, 글자 크기, 여백으로 구조를 만든다.
- 태그는 배지 대신 조용한 텍스트 목록으로 표시한다.

### 3. 유형과 상태를 섞지 않는다

- 유형은 섹션을 결정한다.
- 상태는 작은 점과 텍스트로만 표시한다.
- 진행 상태 때문에 서로 다른 성격의 프로젝트가 한 그룹에 섞이지 않게 한다.

### 4. 단일 강조색

- 라이트 모드는 흰색과 거의 검은 텍스트를 기본으로 한다.
- 다크 모드는 중립적인 검은색 표면과 밝은 텍스트를 기본으로 한다.
- 두 테마 모두 절제된 파란색 하나만 링크, 현재 위치, 완료 상태에 사용한다.

### 5. SF 계열 타이포그래피

- 시스템의 SF Pro 계열 폰트 스택을 유지한다.
- 본문은 Regular, 프로젝트 제목은 Semibold를 기본으로 한다.
- 히어로만 Light를 사용해 크기는 크지만 무겁지 않게 만든다.
- 모노스페이스 장식용 라벨은 사용하지 않는다.

## 상호작용

- 링크는 색 변화와 밑줄만으로 반응한다.
- 요소가 떠오르거나 빛나는 카드 호버 효과는 사용하지 않는다.
- 키보드 포커스는 파란색 외곽선으로 명확하게 표시한다.
- 모바일에서는 유형 제목과 프로젝트 목록을 한 열로 쌓는다.

## 테마 토큰

색상은 `_data/theme_dark.yml`과 `_data/theme_light.yml`에서 관리한다.
`_sass/emergence/_variables.scss`에는 데이터가 로드되지 않을 때를 위한 동일한
fallback이 있다.

과거 Deep Glacier / Stellar Blue 디자인은 보관 브랜치
`orginal-emergence-deep-glacier-stellar-blue`에 보존되어 있으며 활성 디자인의
근거로 사용하지 않는다.
